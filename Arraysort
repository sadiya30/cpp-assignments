#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main()
{
	int a[] = { 11, 5, 80, 29, 7, 13, 45, 22 };
	int n = sizeof(a) / sizeof(a[0]);
	sort(a, a + n);
	for (int i = 0; i < n; ++i)
	cout << a[i] << "\t ";
	return 0;
}

